version: "3.8"

services:
  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crack-detector-api
    ports:
      - "8000:8000"
    volumes:
      - ./app 
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
